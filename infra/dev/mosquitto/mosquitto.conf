# EaglesEye dev MQTT broker — DEV ONLY configuration.
# Anonymous access is fine on localhost; production MQTT gets TLS + per-device credentials.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
